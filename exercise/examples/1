def find_all(lst):
    i=0
    result = -1 
    while (i<len(lst)):
        if (type(lst[i])==int or type(lst[i])==float):
            result = i
            break 
        i+=1
    return result 
print (find_all(["a","b","c"]))
print (find_all([1,2,3]))
print (find_all(["a","b","c",4]))


